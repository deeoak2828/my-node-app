apiVersion: v1
kind: Service
metadata:
  name: my-node-app-service
  namespace: qa
spec:
  type: ClusterIP  # Change from LoadBalancer to ClusterIP
  selector:
    app: my-node-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
